// Simple script just to highlight nav / can be extended later

document.addEventListener("DOMContentLoaded", () => {
  const body = document.body;
  const page = body.dataset.page;

  // Highlight header nav based on anchors (only on home)
  if (page === "home") {
    // nothing special yet, but ready for future scroll spy
  }

  // You can add more interactive behavior later (modals, etc.)
});
